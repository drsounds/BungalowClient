<Page
    x:Class="Bungalow.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bungalow"
    xmlns:bungalow="using:Bungalow.Models"
    xmlns:spotify="using:Bungalow.Spotify.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView OpenPaneLength="300" IsPaneOpen="True" DisplayMode="Inline" CompactPaneLength="10" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <SplitView.Pane>
            <ListView ItemsSource="{x:Bind ViewModel.MenuItems}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="bungalow:MenuItem">
                        <TextBlock Text="{x:Bind Text}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </SplitView.Pane>
        <SplitView.Content>
            <Frame Name="ViewStack"></Frame>
        </SplitView.Content>
    </SplitView>
</Page>
