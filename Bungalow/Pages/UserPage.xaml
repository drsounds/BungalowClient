<Page
    x:Class="Bungalow.Pages.UserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bungalow.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:bungalow="using:Bungalow.Controls"
    xmlns:spotify="using:Spotify.Web.Models"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Padding="28" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <bungalow:Header Data="{x:Bind ViewModel.User}" />
            <TextBlock Text="Public playlists" />
            <GridView ItemsSource="{x:Bind ViewModel.Playlists.Items}" >
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="spotify:Playlist">
                        <bungalow:CoverControl Model="{x:Bind}" />
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>
    </ScrollViewer>
</Page>
