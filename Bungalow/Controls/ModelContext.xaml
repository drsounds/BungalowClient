<UserControl
    x:Class="Bungalow.Controls.ModelContext"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bungalow.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:spotify="using:Spotify.Web.Models"
    xmlns:bungalow="using:Bungalow.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <GridView ItemsSource="{x:Bind Models.Items}" SelectionChanged="GridView_SelectionChanged">
        <GridView.ItemTemplate>
            <DataTemplate x:DataType="spotify:Model">
                <bungalow:CoverControl Model="{x:Bind}" />
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</UserControl>
