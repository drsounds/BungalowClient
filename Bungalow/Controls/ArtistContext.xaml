<UserControl
    x:Class="Bungalow.Controls.ArtistContext"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bungalow.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:spotify="using:Spotify.Web.Models"
    mc:Ignorable="d"
    xmlns:bungalow="using:Bungalow.Controls"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <GridView ItemsSource="{x:Bind Artists.Items}" SelectionChanged="GridView_SelectionChanged">
        <GridView.ItemTemplate>
            <DataTemplate x:DataType="spotify:Artist">
                <bungalow:CoverControl Model="{x:Bind}" />
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</UserControl>
